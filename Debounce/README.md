## 요구사항 정리 및 접근방법
이벤트가 연속으로 발생할때 일정 시간동안 기다렸다가 마지막 한번만 실행되어야 하는 상황,
이벤트가 발생할 때마다 기존 타이머를 clearTimeout으로 취소하고, 일정시간 동안 추가 입력이 없을때만 콜백이 실행되도록 구현.

setTimeout 안에서는 func.apply() 로 원래 컨텍스트와 인자를 유지한 채 함수를 실행하고, 실행 이후에는 타이머를 NULL 로 초기화해 다음 입력을 대기 상태로 만들음. 이렇게 하여 입력이 완료된 시점에만 로직이 실행되도록 설계해야함.